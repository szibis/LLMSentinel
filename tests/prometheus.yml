global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'claude-escalate-monitor'

scrape_configs:
  - job_name: 'gateway'
    static_configs:
      - targets: ['gateway:8077']
    metrics_path: '/metrics'
    scrape_interval: 10s
    scrape_timeout: 5s
